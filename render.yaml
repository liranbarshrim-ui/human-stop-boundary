services:
  - type: web
    name: dar-external-authority
    runtime: python
    plan: free
    buildCommand: "python -m pip install --upgrade pip"
    startCommand: "python dar_v36_14/external_authority_server.py"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
